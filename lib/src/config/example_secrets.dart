/// Copy this file to secrets.dart only when connecting authorized services.
/// Never commit private credentials to a public repository.
class ExampleSecrets {
  static const marketDataApiKey = 'REPLACE_ME';
  static const secureBackendUrl = 'REPLACE_ME';
  static const pocketOptionIntegration = 'NOT_CONFIGURED';
}
